# DIRS
STATE_DIR="${SCRIPT_PARENT}/state"

# CACHE
CACHE_TTL_HOURS=1

# WHITELIST
WHITELIST="${SCRIPT_PARENT}/whitelist.txt"

# MAIL
MAIL_ALERT=0
MAIL_TO=""
MAIL_SUBJECT="[$(hostname)] [${APP_NAME}]"

# LOG
LOG_LVL=6
LOG_TO_CONSOLE=1
# Set this to 1 if script is run by systemd service:
LOG_TO_CONSOLE_WITH_LVL=1